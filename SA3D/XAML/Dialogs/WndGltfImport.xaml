<Window x:Class="SATools.SA3D.XAML.Dialogs.WndGltfImport"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SATools.SA3D.XAML.Dialogs"
        mc:Ignorable="d"
        Title="Import GL Transmission File" Height="200" Width="400"
        ResizeMode="NoResize">
    <Border Padding="10">
        <Grid>
            <Grid.Resources>
                <Style TargetType="{x:Type TextBlock}">
                    <Setter Property="FontWeight" Value="DemiBold"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                </Style> 
            </Grid.Resources>
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="20"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="File"/>
            <TextBox Grid.Row="0" Grid.Column="1" x:Name="PathDisplay" Text="Path" IsReadOnly="True" IsEnabled="False" VerticalContentAlignment="Center"/>

            <TextBlock Grid.Row="1" Text="Generate Normals"/>
            <CheckBox Grid.Row="1" Grid.Column="1" 
                      x:Name="ImportTextures" 
                      IsChecked="True" 
                      VerticalAlignment="Center" 
                      ToolTip="Generate Normals if none exist"/>
            
            <TextBlock Grid.Row="2" Text="Textures"/>
            <CheckBox Grid.Row="2" Grid.Column="1" 
                      x:Name="GenerateNormals" 
                      IsEnabled="False"
                      IsChecked="False" 
                      VerticalAlignment="Center" 
                      ToolTip="Import Textures"/>

            <TextBlock Grid.Row="3" Text="Animations"/>
            <CheckBox Grid.Row="3" Grid.Column="1" 
                      x:Name="ImportAnims" 
                      IsChecked="True" 
                      VerticalAlignment="Center" 
                      ToolTip="Import animations"/>
            
            <TextBlock Grid.Row="4" Text="Framerate"/>
            <TextBox Grid.Row="4" Grid.Column="1" 
                     x:Name="AnimFrameRate" 
                     PreviewTextInput="AnimFrameRate_PreviewTextInput" 
                     Text="60" 
                     MinWidth="30" 
                     Width="auto" 
                     ToolTip="GLTF requires a reference framerate to calculate the keyframes. Our Default is 60"
                     IsEnabled="{Binding ElementName=ImportAnims, Path=IsChecked}" 
                     VerticalContentAlignment="Center" 
                     HorizontalContentAlignment="Right" 
                     HorizontalAlignment="Left"/>

            <StackPanel Grid.Row="5" Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <Button Content="Import" Width="80" Margin="10 0" Click="Import"/>
                <Button Content="Cancel" Width="80" Click="Cancel"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>